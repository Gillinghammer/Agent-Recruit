extends layout

block content
  div(class="inner cover")
    h3(class="cover-heading") Fine Print
    form(action="./4", method="post")
      .form-group
        br
        label(for='answer') You'll find the 7 digit code on the back of your new book
        input#answer.form-control.input-lg(type='text', name="data[answer]")
      button.btn.btn-primary.btn-lg(type='submit') Submit
  br
  p(class="text-danger") #{ msg }
  script.
    $(document).ready(function(){
      $.post( "/start", function( data ) {
        console.log('req sent to start timer')
      });
    });